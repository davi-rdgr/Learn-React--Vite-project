import React from 'react'

const Message = () => {
    const Message = ({ msg }) => {

        return (
            <div>
                <p>a mensagem é: {msg}</p>
            </div>
        )
    }
}

export default Message
